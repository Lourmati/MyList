<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/dialog_rl"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    tools:context=".MainActivity"
    >

    <TextView
        android:id="@+id/dialog_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#ffa31a"
        android:padding="10dp"
        android:text="ADD ITEM"
        android:textAlignment="center"
        android:textColor="#ffFFFF"
        android:textColorHighlight="@color/colorAccent"
        android:textSize="15dp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/et_add"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/dialog_title"
        android:hint="Enter New Item"
        android:textSize="18sp" />

    <Button
        android:id="@+id/dialog_add_btn"
        android:layout_width="140dp"
        android:layout_height="60dp"
        android:layout_below="@id/et_add"
        android:layout_alignParentRight="true"
        android:layout_alignRight="@id/et_add"
        android:background="#ffcc00"
        android:text="Add" />

    <Button
        android:id="@+id/dialog_cancel_btn"
        android:layout_width="140dp"
        android:layout_height="60dp"
        android:layout_alignBottom="@id/dialog_add_btn"
        android:layout_alignLeft="@id/et_add"
        android:layout_marginRight="20dp"
        android:background="#ffcc00"
        android:text="Cancel" />
</RelativeLayout>